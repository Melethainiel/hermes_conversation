services:
  hermes-wrapper:
    build: ./wrapper
    container_name: hermes-wrapper
    restart: unless-stopped
    ports:
      - "8000:8000"
    volumes:
      # Hermes config (API keys, model, memory, personality, etc.)
      - ~/.hermes:/root/.hermes:ro
