services:
  hermes-wrapper:
    build: ./wrapper
    container_name: hermes-wrapper
    restart: unless-stopped
    ports:
      - "8000:8000"
    volumes:
      # Hermes reads its config (API keys, LLM, memory, etc.) from here
      - ~/.hermes:/root/.hermes
